# 执行计划

小改动使用会话内简短计划。跨模块、迁移、架构选择或长时间任务，在 `active/` 创建执行计划；完成后移入 `completed/`。

建议结构：

```markdown
# 任务名称

## 目标与非目标

## 验收标准

## 实施步骤与进度

## 关键决策及原因

## 风险与回滚

## 验证命令及结果
```

计划应随任务更新，而不是只在开始时编写。不得把密钥、凭据或其他敏感信息写入计划。
