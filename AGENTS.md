# AGENTS.md

本文件是智能体进入仓库时的导航地图，不是项目百科全书。保持简短、稳定、可执行；详细知识放在仓库内按需读取的专题文档中。

## 0. 首次初始化

若本文件仍包含 `<填写`，说明项目尚未完成初始化。首次任务开始前：

1. 检查代码、README、构建文件、包清单、CI 和现有测试，收集可以验证的项目事实。
2. 向用户确认无法从仓库可靠推断的产品目标、设计原因和高风险边界。
3. 用真实信息替换全部占位项；命令必须实际存在并可直接执行。
4. 删除不存在且当前不需要的文档入口，不为填满目录而创建空文档。
5. 运行 `pwsh -File ./scripts/check-template.ps1` 或 `bash ./scripts/check-template.sh`，直到检查通过。

初始化只能填写有证据的事实。无法确认的内容必须标记为待确认或询问用户，不得静默猜测。

## 1. 指令优先级

1. 用户当前任务、明确约束和验收标准。
2. 距离目标文件最近的下级 `AGENTS.md`。
3. 本文件。
4. 仓库内已版本化的产品、架构和设计文档。

发现冲突、关键事实缺失或多种解释会显著改变结果时，先说明已知事实、假设和影响，再请求澄清。

## 2. 项目速览（WHAT / WHY）

- 产品：`<填写：一句话说明项目解决什么问题>`
- 技术栈：`<填写：语言、框架、运行时、包管理器>`
- 核心边界：`<填写：最重要的模块职责或依赖方向>`
- 关键原因：`<填写：无法仅从代码表面推断的设计取舍>`
- 高风险区域：`<填写：认证、支付、数据迁移、发布等>`

这里只写大多数任务都会用到、且无法可靠从代码推断的信息。基础语法、普通编码风格和可由工具检查的规则不要重复写在这里。

## 3. 仓库地图与渐进式披露

开始工作时只读取与当前任务有关的入口，不要一次性加载全部文档，也不要把长文档永久导入启动上下文。

| 任务或主题 | 先读取 |
| --- | --- |
| 系统边界、模块职责、依赖方向 | `ARCHITECTURE.md` |
| 产品行为与验收标准 | `docs/product-specs/index.md` |
| 设计原则与长期决策 | `docs/design-docs/index.md` |
| 前端或设计系统改动 | `docs/FRONTEND.md` |
| 安全、认证或权限改动 | `docs/SECURITY.md` |
| 可靠性、性能或可观测性 | `docs/RELIABILITY.md` |
| 复杂或跨模块任务 | `docs/exec-plans/README.md` 与 `docs/exec-plans/active/` |
| 数据库或其他生成内容 | `docs/generated/` 及其来源文件 |

入口不存在时先从代码、测试、构建配置和 CI 确认现状。只有可复用知识需要沉淀时才增加最小文档入口。

工具专属文件（如 `CLAUDE.md`、`GEMINI.md`）只做薄适配，权威规则始终保存在本文件，避免多个版本逐渐分叉。

## 4. 精确命令（HOW）

命令必须能够直接执行。不要写“注意测试效率”之类的愿望，要写清具体命令、范围和运行时机。

| 操作 | 命令 |
| --- | --- |
| 安装依赖 | `<填写>` |
| 启动开发环境 | `<填写>` |
| 运行单个或相关测试 | `<填写>` |
| 格式检查 | `<填写>` |
| 静态检查 | `<填写>` |
| 类型检查 | `<填写>` |
| 集成或端到端测试 | `<填写>` |
| 提交前完整验证 | `<填写>` |

占位项未填写时，从包清单、构建文件、CI 和已有脚本中发现真实命令。找不到就说明缺口，不要自行发明。

## 5. 开始任务

1. 用一句话重述目标，把模糊要求转成可验证的验收标准。
2. 检查工作区状态，区分用户已有修改与本次任务修改；不得覆盖无关改动。
3. 查阅最近的 `AGENTS.md`、相关专题文档、实现和测试。
4. Bug 修复先复现；功能改动先确认当前行为和预期差异。
5. 给出满足目标的最小计划，并为每一步写明验证方式。

小改动使用简短内联计划。涉及多个模块、迁移、架构选择或长时间执行时，在 `docs/exec-plans/active/` 创建计划，持续记录目标、非目标、进度、决策、风险和验证结果；完成后移入 `completed/`。

## 6. 实施原则

- 人类定义意图、边界和验收标准；智能体负责实现、测试和验证。
- 优先沿用仓库已有模式，选择能够解决问题的最小方案。
- 只修改任务直接需要的内容；不顺手重构、重命名或格式化无关文件。
- 不为“以后可能需要”增加功能、抽象层、配置项或兼容分支。
- 在系统边界解析和验证外部数据；使用已确认的 schema 或类型化 SDK，不猜测字段结构。
- 遵守架构依赖方向；关键边界优先用 lint、类型或结构测试强制执行。
- 修改生成内容时改其来源并重新生成，不手工编辑生成文件。
- 引入新依赖前说明现有能力为何不足，并优先选择稳定、可检查、仓库内易推理的接口。
- 注释只解释“为什么”和非显然约束，不复述代码。
- 每条新增的“不要”必须同时给出正确替代路径和必要原因。

## 7. 验证闭环（VERIFY）

1. 先运行最接近改动的测试，确认问题修复或行为符合验收标准。
2. 再运行受影响范围的格式、lint、类型检查和测试；全量检查只在风险或仓库流程要求时运行。
3. UI 改动必须实际启动应用并验证关键用户路径；可用时检查 DOM、截图、控制台和网络请求。
4. 服务改动同时检查启动、日志、指标、追踪、超时和错误路径，而不只验证成功响应。
5. 配置、数据库或发布改动必须说明兼容性、迁移和回滚影响。
6. 对照验收标准逐项确认，并审阅最终 diff 是否包含无关变化。

不得声称未实际运行的检查已经通过。无法运行时，说明原因、替代验证和剩余风险。

## 8. 评审、权限与安全

- 自审正确性、边界条件、并发、重试、幂等性、安全、可维护性、可观测性和回归测试。
- 不泄露密钥、凭证或敏感数据，不把真实秘密写入代码、日志、测试或文档。
- 不通过关闭测试、放宽断言、添加跳过标记或降低质量门槛获得绿色结果。
- 未经明确授权，不发布、合并、删除数据、修改权限或执行其他不可逆操作。
- 若评审意见反复出现，将其转化为测试、lint、脚本或权威文档，而不只是修当前补丁。

## 9. 上下文与会话连续性

长任务或上下文压缩前，必须保留：当前目标与验收标准、已修改文件、计划进度、已运行命令及结果、未解决问题和下一步动作。

不要把关键状态只留在聊天中。可跨会话复用的事实写入仓库文档；临时但复杂的状态写入活跃执行计划。

## 10. 仓库即记录系统

- 仓库内已版本化的代码、文档、schema、测试和计划是事实来源。
- 每个事实只设一个权威位置，其他位置使用链接，不复制整段内容。
- 行为、接口、架构或运维方式变化时，同步更新对应权威文档。
- 新的失败模式必须进入反馈回路：测试复现、文档记录，或由 lint、类型、CI、脚本直接阻止。
- Agent 如果无法自行读取 UI、日志、指标或测试结果，应优先补齐最小工具入口，而不是长期依赖人工复制粘贴。

## 11. 完成定义

- 验收标准逐项满足，改动范围保持最小。
- 没有覆盖用户的无关修改，也没有遗留调试代码、跳过标记或无主 TODO。
- 相关检查已通过，或未运行项及风险已明确列出。
- 必要的测试、文档、生成内容和执行计划已同步。
- 最终说明包含：做了什么、为什么、如何验证、还有哪些风险或后续事项。

## 12. 维护本文件

把本文件当成高信噪比的错题本，而不是规则收藏夹：真实事故暴露出通用缺口时才新增规则；规则可机械执行时就迁移到 lint、测试或脚本；定期删除过时、重复或可从代码直接推断的内容。

本文件应保持在约 100 行量级。需要大量解释的内容移入专题文档并在此提供条件入口，防止它重新变成一本失真的千页说明书。
